Quiz 1 - 4:05:00

1 - What does the '.map()' array method do?
Ans - Go through each element of the given array and return a new array.
Bob Ziroll Answer - Returns a new array. Whaever gets returned from the callback function provided is placed at the same index in the new array.

2 - What do we usually use '.map()' for in React?
Ans - To parse the array.
Bob Ziroll Answer - Conver an array of raw data into an array of JSX elements that can be displayed on the page.

3 - Why is using '.map()' better than just creating the components manuall by typing them out?
Ans - Becasue it return an array of elements (components) in the response, which we can use to pass to the parent component.
Bob Ziroll Answer - It makes our code more "self-sustaining" - not requiring additional changes whenever the data changes.
